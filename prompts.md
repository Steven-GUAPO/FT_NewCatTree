Goal: Visit filmtools.com and scrape their Storage department, extract structured attributes from messy product titles/descriptions/spec-tables, and propose two innovative user-first category tree ideas to choose from (not their existing one) with auto-assignment plus a small faceted demo. Before diving into the code, fetch one Storage category page and one product page and tell me how each is structured and what data each exposes. Then propose an approach and project layout and ask me any scoping questions. Don't build yet.
Add a scraper/ package: fetch, parse, attributes, taxonomy, pipeline, plus __init__ and __main__. Add output/, demo/ and requirements.txt.
Implement fetch.py a conservative crawler. The category grid is client-side rendered, so enumerate product URLs with headless Playwright, paginating until no new tiles appear. Product detail pages are server-rendered, so fetch those with requests. Make sure to obey their scrape policy, send one request at a time with randomized 1–3s delays, and an on-disk HTML cache.
Implement parse.py which turns a product-detail page into a raw record {url,title,brand,mpn,sku,price_usd,description,specs}.
Do a small live crawl (cap at 30 products spanning SSD, RAID, memory cards, LTO, docks, cables) and save the raw records to data/sample_raw.json, so I can develop the offline pipeline against a real fixture without re-crawling.
Implement attributes.py that extracts normalized attributes from title+description+specs: brand, media_type, form_factor, capacity, interfaces (with max Gb/s), speed (read/write MB/s, rpm), bays/raid, rugged (IP rating + drop height), encryption, bus_powered, warranty, lto, card spec. Each field returns value + provenance (specs > title > description) + confidence values. Normalize to canonical units (capacity → GB int). Add QA notes for disagreements and a completeness score.
Implement taxonomy.py as three layers: (1) primary spine keyed on object type — Drives / Multi-Bay & RAID / Camera Media / Readers & Docks / NAS / Archive & Backup / Recorders / Cables; (2) orthogonal facets (media, form, interface, capacity bucket, speed tier, rugged, bays, brand); (3) workflow saved-queries (On-Set Capture, Offload & Ingest, Edit Fast, Backup & Redundancy, Archive Forever, Team & Network). Deterministic assignment: one primary path (≥2 bays or RAID → Multi-Bay regardless of the product-line name), all applicable facets, multi-membership workflows, plus assignment_confidence and a needs_review flag routing anything unplaceable or too sparse to Uncategorized → Needs Review.
Implement pipeline.py that includes process_records (attributes→taxonomy per product), build_summary (category/workflow/media/interface counts, extraction coverage %, review queue), write_outputs (products.json, products.csv flat view, summary.json, demo_products.json). Share these stages across --offline and --live. Then run --offline on the sample and show me the summary.
Audit the sample output for extraction bugs.
Run the full Storage department live crawl to output/, keeping the polite delays. Report product count, extraction coverage, and review-queue size.
Build a demo/faceted_demo.html. The idea is a self-contained faceted browser over demo_products.json with the data embedded inline (no server, no build step, no localStorage). Include a left sidebar of stackable facets with live per-value counts, a workflow filter, removable active-filter chips, sort, and product cards showing the auto-assigned primary path, key facet chips, price, and a completeness meter / review badge. Dark palette (preferred), one type family with tabular numerals, responsive and keyboard-accessible.
